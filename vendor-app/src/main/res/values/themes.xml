<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Framework Material parent (no extra dependency). Compose draws its own theme. -->
    <style name="Theme.Vendor" parent="@android:style/Theme.Material.Light.NoActionBar" />
</resources>
